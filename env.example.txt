# Anthropic
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-sonnet-4-6

# Shared secret Retool (or anything else) must send as x-api-key on every request.
# Generate your OWN new random value for this agent — don't reuse Wellington's.
API_KEY=

# Simpro Auckland API — get these directly from Auckland's own Simpro instance
# (Setup > API), the same way we got Wellington's.
SIMPRO_BASE_URL=https://your-auckland-simpro-instance.simprosuite.com/api/v1.0/
SIMPRO_BEARER_TOKEN=
SIMPRO_COMPANY_ID=

# Service
PORT=3002
